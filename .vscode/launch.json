{
    "configurations": [
    {
        "name": "Launch Chrome",
        "request": "launch",
        "type": "firefox",
        "url": "http://localhost:5173",
        "webRoot": "${workspaceFolder}"
    }
    ]
}